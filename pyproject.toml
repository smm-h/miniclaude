[project]
name = "miniclaude"
version = "0.1.0"
description = "A lean, snappy inline terminal client for Claude Code"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
dependencies = [
    "claudestream",
    "strictcli",
    "prompt-toolkit",
]
keywords = ["rlsbl"]

[project.scripts]
miniclaude = "miniclaude._cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "pytest",
    "pytest-timeout",
]

[tool.pytest.ini_options]
markers = [
    "integration: end-to-end tests that spawn a real Claude CLI in a pty (slow, network)",
]
